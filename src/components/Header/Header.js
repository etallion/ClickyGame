import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <div className="header">
           <h1>Try not to click the same color twice!</h1>
           <h2>Scroll to the bottom, Good Luck!</h2>
        </div>
    );
};

export default Header;